commands:

  - name: "SSMTP configured to use local Mailcatcher"
    command: "sudo cat /etc/ssmtp/ssmtp.conf | grep 'mailhub=localhost:1025' || echo 'not-configured'"
    not_expected: "not-configured"